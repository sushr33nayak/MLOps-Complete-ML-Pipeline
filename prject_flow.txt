Building Pipeline:
1> Create a github repo and clone it in local(Add experiments folder)
2>Add src folder along with all components(run them indivisually)
Add data, model,report directories to .gitignore file
Now git add, commit push